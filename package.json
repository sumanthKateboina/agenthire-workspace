{
  "name": "agenthire-workspace",
  "version": "1.0.0",
  "description": "Workspace manager for AgentHire AI recruitment agent platform",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client --legacy-peer-deps",
    "dev": "npx concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npx concurrently \"npm start --prefix server\" \"npm start --prefix client\"",
    "seed": "node scripts/seed-resumes.js && node server/seedDb.js"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
